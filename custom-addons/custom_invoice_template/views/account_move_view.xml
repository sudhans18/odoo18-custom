<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_server_print_custom_invoice" model="ir.actions.server">
            <field name="name">Custom Invoice (PDF)</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_type">action</field>
            <field name="state">code</field>
            <field name="code">
action = env["account.move"].browse(context.get("active_ids")).action_print_custom_invoice()
            </field>
        </record>

        <record id="view_move_form_custom_invoice_button" model="ir.ui.view">
            <field name="name">account.move.form.custom.invoice.button</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='button_box']" position="inside">
                    <button name="action_print_custom_invoice"
                            type="object"
                            string="Custom Invoice"
                            class="oe_stat_button"
                            icon="fa-print"
                            modifiers='{"invisible": ["|", ["move_type", "not in", ["out_invoice", "out_refund"]], ["state", "=", "draft"]]}' />
                </xpath>
            </field>
        </record>
    </data>
</odoo>
